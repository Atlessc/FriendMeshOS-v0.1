#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
exec "$HOME/.platformio/penv/bin/python" \
    "$ROOT_DIR/.pio/libdeps/t-deck-tft/Nanopb/generator/nanopb_generator.py" \
    --protoc-plugin "$@"
